Lets fix some bugs.
We have implemented, that if the user does a swap and the passively swapped tile has a special feature (like free_swap_vertical) it does not consume the special feature.
But if the swap is an invalid swap, the free_swap should take effect and be consumed.

---

Do you think we can refactor the exectueSwap a little?
Looks to me, that we have duplicated code for tile1 and tile1

---

Do you think we can refactor the exectueSwap a little?
Looks to me, that we have duplicated code for tile1 and tile2

---

Ok perfect. Next bug is, that the teleport icon in the special tile ::after is to big on desktop. Because the font-size is 8px, 2vw, 16px @styles/game.css 
Can we maybe size that with cqw based on the .gem container?

---

Ok next: For every other goal than the blocked goals (eg. current, cursed)
In the goals UI, instead of just showing how many tiles you are left to create show "0/2"

---

Can you add a little bit more letter spacing